# Nginx 設計 検討まとめ（COSMO 電文受付・ファイルDL・本部画面）

本書は、これまでの連携資料（方式設計書・PoC実ソース・Confluence/JIRA）を1本に統合した検討資料。
**最優先資料 = 方式設計書『【COSMO_Ph0】SSD-010_処理方式設計書_方式設計_v1.00.10.pptx』**。矛盾なき限りこれに従う。

- 個別の詳細: `docs/06-method-spec.md`(方式), `docs/07-discrepancies.md`(相違), `docs/01`(カナリア), `docs/08`(ログ) 他
- 画像インデックス: `sources/README.md`（計81枚 = 第1弾29 + 第2弾19 + 第3弾33）
- PoC復元ソース: `reference/poc-source/`
- 「出典」列の `IMG_xxxx` は `sources/images/` の画像、`4-2-x` は方式設計書の章番号、`pXX` はスライドのページ番号。

---

## 0. 方式設計書 章↔ページ対応表

> スライド右下のページ番号を写真から判読。**写り込みが無いものは「要確認」**（現物PPTXで確認要）。
> 章番号は確実、ページ番号は判読できた範囲。

| 章 | 内容 | ページ | 画像 | 状態 |
|----|------|-------|------|------|
| 4-2-2 | 電文受付機能について | p? | `IMG_5385` | 要確認（p70より前）|
| 4-2-7 | 流量制御方式（電文受付）| p? | `IMG_5386/5387` | 要確認 |
| 4-2-8 | タイマー制御方式（電文受付）| p? | `IMG_5388/5389/5390` | 要確認 |
| 4-2-10 | エラーハンドリング方式（電文受付）| p? | `IMG_5391〜5395` | 要確認 |
| 4-2-13 | ログ出力方式 | p? | `IMG_5396/5397/5398` | 要確認 |
| 4-2-17 | ファイルアップロード方式 | p70前後 | `IMG_5399` | 要確認 |
| **4-2-18** | **ファイルダウンロード方式** | **p70** | `IMG_5399` | ✅判読 |
| **4-3-4** | **負荷分散方式（画面）** | **p81** | `IMG_5400` | ✅判読 |
| 4-3-6 | 流量制御方式（画面）| p? | `IMG_5401` | 要確認（p81〜89間）|
| 4-3-7 | タイマー制御方式（画面）| **p89** | `IMG_5402/5403/5404` | ✅判読（相関図がp89）|
| **4-3-9** | **エラーハンドリング方式（画面）** | **p92** | `IMG_5405〜5409` | ✅判読 |
| 4-3-11 | リトライ方式（画面）| p10x | `IMG_5410` | 要確認（下1桁不明瞭）|
| 4-3-14 | 認証・認可方式（画面）| p? | `IMG_5411` | 要確認 |
| 4-3-16 | ファイルアップロード方式（画面）| p? | `IMG_5412/5413` | 要確認 |
| 4-3-17 | ファイルダウンロード方式（画面）| p? | `IMG_5414/5415` | 要確認 |
| 4-3-19 | 閉塞制御方式（画面）| p11x | `IMG_5416/5417` | 要確認 |

※ ファイル名: `【COSMO_Ph0】SSD-010_処理方式設計書_方式設計_v1.00.10.pptx`

---

## 1. 全体像

```
                                COSMO API基盤
  SC/新POS ─S-NET(HTTPS)─▶ 電文受付機能                    他領域
                          ├ ①ELB ─▶ ECS ┌ nginx①(通常API)┐ ─▶ ④ELB ─▶ ⑤ECS(API/Tomcat) ─▶ ⑥Aurora
                          │              └ nginx②(ファイルDL)┘ ─▶ S3
                          （java=カナリア対象アプリ）

  LAW端末 ─(SSL-VPN/DX)─▶ ALB(OIDC認証) ─▶ ECS(Webサーバ nginx) ─▶ S3(静的/UL/DL) , API ─▶ Aurora/ElastiCache
                                                                （画面系=React）
```

**Nginx は「3つの conf」= 3文脈で登場**（Confluence の3対象と一致）。出典: 4-2-2 `IMG_5385` / 4-3全体。

| # | conf対象 | 役割 | 主な出典 |
|---|---------|------|---------|
| (1) | **電文受付①（通常API）** | njs カナリアリリース + 流量制御 + アイドルTO + 404固定JSON | 4-2 / JIKIPH0-220 |
| (2) | **電文受付②（ファイルDL）** | 署名付きURLを S3 へ中継（ホスト名置換）+ 下り帯域制御 | 4-2-18 `IMG_5399` |
| (3) | **画面系（React）** | S3バック静的配信 + index.html fallback + S3 UL/DL中継 | 4-3 |

---

## 2. (1) 電文受付① 通常API（njs カナリアリリース）

### 方式・仕様

| 項目 | 内容 | 出典 |
|------|------|------|
| URL構造 | `https://ドメイン/コンテキストパス/v1/API名` | 4-2-2 `IMG_5385` |
| 振分け | コンテキストパスでターゲット振分け。**カナリア時はBODYの店舗コードで振分け** | 4-2-2 `IMG_5385` |
| エラー(TO) | nginx タイムアウト → **504 Gateway Timeout**（デフォルト挙動） | 4-2-10 `IMG_5393` / 4-2-8 `IMG_5390` |
| エラー(呼出先) | 呼び出し先から返ったエラーは**そのまま返却** | 4-2-10 `IMG_5393`(1-6) |
| 未定義パス | 存在しないコンテキストパス → **404固定JSON** | JIKIPH0-2288 `IMG_5012/5013` |
| タイマー | T3 nginx→ALB、**nginx設定ファイル**で設定、504返却 | 4-2-8 `IMG_5390` |

### PoC 実装（参考、`reference/poc-source/`）

- 方式B: `js_set $upstream route.pick_upstream;` → `proxy_pass $upstream;`（`IMG_5368`）
- loader.py が Aurora の `routing`/`default_routing` を読み `routing.json` 出力 → entrypoint が `routes.js` 生成（`IMG_5369/5382/5383`）
- route.js: キー生成し stores→defaults→DEFAULT で解決、start_at/end_at で時間指定リリース（`IMG_5371〜5375`）

### TODO（① 通常API）

- [ ] **【最重要/相違A】振分けキー = BODYの店舗コード**。PoCは`X-Shop-Id`ヘッダー。njsで**リクエストBODY**を読む設計へ。／ 出典: 4-2-2 `IMG_5385`、相違: `docs/07`
- [ ] **対象テーブル定義**（`routing` / `default_routing` の正式スキーマ）※テーブル定義書と突合。／ PoC: `IMG_5382/5383`
- [ ] URI の context_path・version 抽出規則の正式化。／ 4-2-2 `IMG_5385`
- [ ] upstreamMap（routing_version→upstream）の**拡張性**。PoCはenvハードコード列挙 → DB化/動的生成。／ PoC: `IMG_5369`
- [ ] 未定義コンテキストパスの **404固定JSON** 実装（無限ループ回避）。／ JIKIPH0-2288、4-2-10(1-6)`IMG_5393`
- [ ] DB認証情報を Secrets Manager 化（PoCは平文env）。／ PoC: `IMG_5379`
- [ ] アイドルTO値（`proxy_read_timeout` 等）の決定。／ 4-2-8 `IMG_5390`、検証 `IMG_5016`

---

## 3. (2) 電文受付② ファイルダウンロード（S3中継）

### 方式・仕様

| 項目 | 内容 | 出典 |
|------|------|------|
| 通常方式 | 配信計画取得で返却したURLに POS がアクセス（署名付き） | 4-2-18 `IMG_5399` |
| URL | `https://ドメイン/download/バケット名/ファイルパス?署名` → nginx が S3ドメインに置換して中継 | 4-2-18 `IMG_5399` |
| オフラインDL | 固定URL `https://ドメイン/download/offline(仮)/固定ファイル名?署名&posVersion=2.0.0` | 4-2-18 `IMG_5399` |
| 署名 | **API側で発行。nginxは署名しない（中継のみ、?署名を保持）** | 4-2-18 `IMG_5399` / 4-3-17 `IMG_5414` |
| 経路 | **VPC Gateway エンドポイント経由**で S3 到達（閉域網） | 4-3-17 `IMG_5414` |
| 帯域制御 | 下り転送速度制限（with 帯域制御ヘッダ） | 4-2-2 `IMG_5385` / 図 `IMG_5394` |
| エラー | S3 4xx/5xx はそのまま返却、クライアントでハンドリング | 4-2-10 `IMG_5395` |

### TODO（② ファイルDL）

- [ ] **【相違E】nginxは署名せず中継**。ホスト名置換（`proxy_pass`+`proxy_set_header Host`）実装。／ 4-2-18 `IMG_5399`
- [ ] 署名クエリ透過の担保（プロキシで署名が壊れないこと）。／ 4-2-18 `IMG_5399`
- [ ] VPC Gateway エンドポイントのルーティング/バケットポリシー。／ 4-3-17 `IMG_5414`
- [ ] `download` コンテキストパスで DL用 nginx へ振り分ける方法。／ 4-2-2 `IMG_5385`
- [ ] 帯域制御値（`limit_rate`/`limit_rate_after`）と「帯域制御ヘッダ」仕様。／ 4-2-2 `IMG_5385`
- [ ] 環境変数化: S3ドメイン/バケット/リージョン/VPC EP/帯域値

---

## 4. (3) 画面系 React（静的配信 + S3中継）

### 方式・仕様

| 項目 | 内容 | 出典 |
|------|------|------|
| 画面技術 | **React で確定**（他領域S3の Angular は別物） | 4-3全体, T1 `IMG_5404` |
| 構成 | ALB → Webサーバ(nginx) → S3(静的) / API → Aurora・ElastiCache | 4-3-4 `IMG_5400` |
| 静的配信 | nginx が S3 をバックに配信 | 4-3-4 `IMG_5400` |
| **SPA fallback** | 存在しないURL → **index.html を 200 で返す（404にしない）** | 4-3-9(1-5) `IMG_5405/5407` |
| 認証 | **ALB で OIDC（Entra ID）**。nginxは認証しない | 4-3-14 `IMG_5411` |
| ファイルUL | API発行URL（ホスト名nginx置換）→ nginx が VPC GW EP 経由でS3中継。物理名=時刻+UUID(上書禁止)、論理↔物理はDB | 4-3-16 `IMG_5412/5413` |
| ファイルDL | 同上（取得元URL、nginx中継） | 4-3-17 `IMG_5414/5415` |
| 閉塞制御 | 初回起動＋操作時に API経由で Aurora の閉塞状態問合せ（nginx直接でない） | 4-3-19 `IMG_5416/5417` |
| 流量制御 | nginx は limit_conn/limit_conn_zone（S3への同時接続制限） | 4-3-6 `IMG_5401` |
| リトライ | 画面→APIはリトライしない、ファイルUL(Nginx)もしない | 4-3-11 `IMG_5410` |

### TODO（③ React）

- [ ] **【相違F】SPA fallback**: `try_files $uri $uri/ /index.html;`（404を返さない）。／ 4-3-9 `IMG_5405/5407`
- [ ] 静的配信方式の具体化（S3を`proxy_pass`配信 or S3同期して`root`）。／ 4-3-4 `IMG_5400`
- [ ] UL/DL の VPC GW EP 中継 + ホスト名置換（②と共通）。／ 4-3-16/17 `IMG_5412〜5415`
- [ ] キャッシュ制御（index.html no-cache / ハッシュ資産 長期）
- [ ] 「アプリごとに微修正で済む」テンプレート粒度。／ Confluence `IMG_5008`

---

## 5. 流量制御（横断）

**方式資料での nginx 流量制御は `limit_conn`/`limit_conn_zone` と worker系のみ。`limit_req`は記載なし。**

| # | コンポーネント | 設定 | 方針 | 出典 |
|---|--------------|------|------|------|
| ① | ELB | - | 設定しない（nginxで制御） | 4-3-6 `IMG_5401` |
| ② | **nginx** | `worker_processes`/`worker_connections` | **デフォルト基本**（auto / 512） | 4-2-7 `IMG_5387` / 4-3-6 `IMG_5401` |
| ② | **nginx** | `limit_conn`/`limit_conn_zone` | **server_name単位**で同時接続制限、デフォルト基本 | 4-2-7 `IMG_5387` |
| ⑤ | Tomcat | maxThreads | 自コンテナ/後続を加味 | 4-2-7 `IMG_5387` |
| ⑤ | HikariCP | maximumPoolSize | 同一DB利用の他アプリに影響させない | 4-2-7 `IMG_5387` |
| ⑥ | Aurora等 | - | 前段で流量制限、設定なし | 4-3-6 `IMG_5401` |

- 適用範囲: POS向けAPIは②nginx+⑤API、内部APIは⑤のみ。ECS内設定値はタスク単位（オートスケール加味）。／ 4-2-7 `IMG_5386`

### TODO（流量制御）

- [ ] **【相違C】worker_connections はデフォルト512基本**（PoCは1024）。／ 4-2-7 `IMG_5387`
- [ ] **【相違D】limit_req は方式外**。基本は limit_conn 系。テンプレの limit_req は削除 or 別途合意。／ 4-2-7 `IMG_5387`
- [ ] 各制限値（conn数）とタスク多重度の考え方
- [ ] limit_conn_zone のキー（$server_name / $uri / IP）の最終決定
- [ ] 超過時ステータス（limit_conn_status）と応答body

---

## 6. ログ出力（横断）

JSON形式。**nginx のログフォーマットは「後続で別途定義」**（＝未確定）。出典: 4-2-13 `IMG_5396/5397/5398`。

- 項目: `timestamp`(JST,yyyy-MM-ddHH:mm:ss.SSS) / `id` / `name` / `level` / `type`(monitor/app/trail) / `thread` / `function`(API名) / `user` / `action` / `subtype` / `trace`(TraceID) / `message`
- subtype: SYSTEM / **NGINX** / AWSCALL / DBCALL / APL
- 種別3系統: 運用監視(monitor,ERROR) / アプリ実行(app,ERROR〜DEBUG) / 証跡(trail,INFO)

### TODO（ログ）

- [ ] **nginx の `log_format` を上記JSON項目に寄せる定義**（後続定義。方式資料でも未確定）。／ 4-2-13 `IMG_5396`
- [ ] nginx変数（`$time_iso8601`/`$status`/`$upstream_addr`/TraceIDヘッダ 等）との対応付け
- [ ] PoC の route.js `r.error()` デバッグログの棲み分け

---

## 7. 横断（テンプレート化・タイマー・脆弱性）

| 項目 | 内容 | 出典 |
|------|------|------|
| テンプレート化 | conf は GitLab管理、**環境変数で環境ごとに切替**。ECS起動時に生成 | Confluence `IMG_5011` |
| conf対象 | 電文受付 / ファイルダウンロード / 本部画面用React | Confluence `IMG_5007/5010` |
| タイマー | T3(nginx→ALB/S3)=nginx設定ファイル、504。クライアントは余裕を持った値 | 4-2-8 `IMG_5390` / 4-3-7 `IMG_5404` |
| 脆弱性 | 処理方式設計書記載 1.28 に脆弱性 → バージョンUP（PoCは1.27） | JIKIPH0-267 `IMG_5015` |

### TODO（横断）

- [ ] **【相違B】conf を3本に分割**（電文受付①/②/画面系）。テンプレート構造を再編。／ 4-2-2 `IMG_5385` ほか
- [ ] 環境変数の命名規約・envsubst展開対象の明示
- [ ] nginx/njs の商用バージョン確定（脆弱性対応）。／ JIKIPH0-267 `IMG_5015`
- [ ] 処理方式設計書の nginx バージョン記載修正

---

## 8. 「残課題はテーブル・ヘッダー・ログだけ？」への結論

概ね作れる状態。ただし方式資料を正とすると、**その3つ以外に相違A/B/E/F の設計判断が残る**:

| 残課題 | 区分 | 出典 |
|--------|------|------|
| 対象テーブル定義 | ユーザー認識どおり | テーブル定義書（未入手）/ PoC `IMG_5382/5383` |
| リクエストの値（=**BODYの店舗コード**）| ユーザー認識＋相違A | 4-2-2 `IMG_5385` |
| ログフォーマット（nginxは後続定義）| ユーザー認識どおり | 4-2-13 `IMG_5396` |
| 相違A: BODY読取りのnjs設計 | 追加 | 4-2-2 `IMG_5385` |
| 相違B: conf 3分割 | 追加 | 4-2-2 `IMG_5385` |
| 相違E: DLは署名付きURL中継（nginx署名不要）| 追加 | 4-2-18 `IMG_5399` |
| 相違F: React 404→index.html | 追加 | 4-3-9 `IMG_5405` |

→ これらを潰せば `nginx.conf.template`（3本）を方式資料準拠で組める。次の一手候補: **相違A（BODYキーのnjs方式）** と **相違B（3 conf構成）** の確定。

---

## 9. 未入手・確認待ちの資料

- [ ] テーブル定義書（電文受付＝`routing`/`default_routing`）
- [ ] React で参照すべき資料（アプリチームへ問い合わせ）／ Confluence 記載
- [ ] API共通仕様書（エラーJSON形式の正）
- [ ] 追加の画面撮影画像（随時）
